import 'package:flutter/material.dart';

class CenteredCircularProgressindicaator extends StatelessWidget {
  const CenteredCircularProgressindicaator({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: CircularProgressIndicator(),
    );
  }
}
